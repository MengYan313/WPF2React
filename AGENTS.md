# AGENTS.md

## 开发原则

本仓库是单人维护的小型实验项目。开发以快速上手、容易理解、方便修改和验证实验为首要目标。

- 在保证当前核心功能可运行的前提下，使用尽可能少的代码、模块和抽象。优先采用直接的数据流、常见数据结构和短函数，删除重复包装、闲置扩展点与过度设计。
- 只支持当前代码产生的最新数据格式。不得保留旧数据集、旧目录、旧字段或旧 Schema 的读取、迁移和兼容分支，也不在项目产物中维护版本字段。
- 不使用 SHA、内容哈希、提交哈希或提示词哈希约束输入、产物、断点续跑和实验流程。使用项目名、仓库相对路径和必要的业务 ID 直接定位数据。
- 尽可能减少防御性编程。只在文件、用户输入、外部进程和 LLM 响应等真实边界保留必要校验；内部流程按当前契约直接处理，不堆叠静默回退、重复检查和多层拒绝。
- 结构化 LLM 调用保留一次必要的 JSON 修复和有限重试，除此之外不增加额外恢复层。资源和 Runtime 使用清晰的 `try/finally` 释放。
- 第三方模型或 API 名称中的固有版本号不属于项目数据版本控制，可以保留。
- 修改实现时同步删除失效的测试和文档约束，只保留当前主流程、关键算法和最小端到端验证。

## 本地依赖

- 使用项目根目录的 `.venv`，不要向系统 Python 安装依赖。

## 项目结构

- `src/parser/`：解析 WPF 项目，输出当前格式的结构与依赖数据。
- `src/migration/`：将解析结果迁移为 React + TypeScript + MUI。
- `src/llm/`：模型客户端、JSON 输出和有限重试。
- `src/common/`：少量共享基础设施；日志统一从 `src.common.logging` 导入。
- `repos/`：本地输入项目，不提交。
- `outputs/`：解析和迁移中间产物，不提交。
- `results/`：最终迁移结果，不提交。
- `docs/research/experiment-page-set.json`：当前唯一实验页集。

新增代码应直接复用 `src.llm`、`src.agents.base` 和 `src.common.logging`，不得再增加兼容导出、模型配置包装层或重复日志入口。

## 验证

从仓库根目录运行：

```bash
.venv/bin/python -m unittest discover -s tests -t . -v
.venv/bin/python scripts/check_shared_infrastructure.py --other ../CodeIdiomMine
```

测试时可以使用适量真实的 LLM 调用进行验证。

## 文档

项目自有文档使用中文；代码、命令、路径、API 名称、JSON 字段和必要术语可保留英文。当前运行方式以根 `README.md` 和各模块 `README.md` 为准，不在文档中维护历史提交、旧版产物或已废弃流程。
